server:
  port: 8044
  servlet:    context-path: /uaa
  use-forward-headers: false

spring:
  application:
    name: authorization-service  profiles:    active:
    - dev
#  cloud:
#    config:
#      discovery:
#        enabled: ${CONFIG_DISCOVERY_ENABLED:false}
#        service-id: ${CONFIG_DISCOVERY_SERVICE_ID:config-service}
#      uri:
#      - ${CONFIG_SERVICE_URL:http://localhost:8888}
#    refresh.enabled: true
#    bus:
#      enabled: true
    
  zipkin.base-url: ${ZIPKIN_BASE_URL:http://localhost:9411/}
  sleuth.sampler.probability: 1
  
eureka.client.service-url:
  defaultZone: ${EUREKA_SERVICE_URL:http://localhost:8761/eureka/}
  
management:
  endpoints:
    web:
      exposure:
        include: '*'
        
